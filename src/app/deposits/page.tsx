import { Metadata } from 'next';
import React from 'react'

export const metadata: Metadata  = {
    title: "Build Deposit",
  };

const Page = () => {
  return (
    <h2>Build Deposit</h2>
  )
}

export default Page